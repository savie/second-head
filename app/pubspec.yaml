name: second_head
description: SECOND HEAD Flutter + Dart application foundation.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  package_info_plus: ^8.3.0
  connectivity_plus: ^7.3.1
  path_provider: ^2.1.5
  file_picker: ^10.3.3
  url_launcher: ^6.3.2
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/unity.png
